export type ActionsSidebarType ={

}

export type SidebarType = {}
export type SidebarReducerType = any

let initialState= {}

export const sidebarReducer = (state: SidebarReducerType = initialState, action: ActionsSidebarType): SidebarType => {
    return state;
}