import {ActionType, SidebarReducerType, SidebarType} from "./state";

let initialState= {}

export const sidebarReducer = (state: SidebarReducerType = initialState, action: ActionType): SidebarType => {
    return state;
}